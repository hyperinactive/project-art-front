import React from 'react';

const ProjectWorkspace = () => {
  const placeholder = 'Project Workspace -- only for members';

  return (
    // TODO: setup the feed
    <div className="projectWorkspace">
      <h1>{placeholder}</h1>
    </div>
  );
};

export default ProjectWorkspace;
